//when accessing multiple types we move the types to the seperate file
export type Name ={
    first: string,
    last: string
}
export type PersonProps = {
    name:{
        first:string;
        last:string;
    }
} 
